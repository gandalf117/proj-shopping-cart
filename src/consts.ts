import { statusTypes } from 'types';

export const LoadingStates = {
  Idle: 'idle' as statusTypes,
  Loading: 'loading' as statusTypes,
  Success: 'success' as statusTypes,
  Fail: 'fail' as statusTypes,
}
